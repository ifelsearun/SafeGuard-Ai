import React, { useState } from 'react';
import { Link } from 'react-router-dom';
import { Trash2 } from 'lucide-react';
import { clearHistory, readHistory } from '../services/history.js';
import { Button, Card, EmptyState, VerdictBadge } from '../components/ui.jsx';
export default function HistoryPage() { const [history, setHistory] = useState(readHistory); const clear = () => { if (window.confirm('Clear all locally saved scan history?')) { clearHistory(); setHistory([]); } }; return <div><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="eyebrow">Local scan history</p><h1 className="page-title">Recent scans</h1><p className="page-description">Saved only in this browser. Clear your data whenever you need to.</p></div>{history.length > 0 && <Button variant="danger" onClick={clear}><Trash2 size={17} />Clear history</Button>}</div><div className="mt-8">{history.length === 0 ? <EmptyState title="No scans saved yet" description="Completed scans will appear here on this device." action={<Link className="text-blue-300" to="/scan/url">Scan a URL →</Link>} /> : <Card className="overflow-auto"><table className="w-full min-w-[680px] text-left text-sm"><thead className="bg-slate-800 text-slate-300"><tr><th className="p-4">Target</th><th className="p-4">Type</th><th className="p-4">Verdict</th><th className="p-4">Date</th><th className="p-4" /></tr></thead><tbody>{history.map((entry) => <tr className="border-t border-slate-700/70" key={entry.analysisId}><td className="max-w-64 truncate p-4 font-medium">{entry.target}</td><td className="p-4 capitalize text-slate-400">{entry.scanType}</td><td className="p-4"><VerdictBadge verdict={entry.verdict} /></td><td className="p-4 text-slate-400">{new Date(entry.scannedAt).toLocaleString()}</td><td className="p-4"><Link className="text-blue-300 hover:text-blue-200" to={`/results/${entry.analysisId}`} state={{ target: entry.target, scanType: entry.scanType }}>Open</Link></td></tr>)}</tbody></table></Card>}</div></div>; }
